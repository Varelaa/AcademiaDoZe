<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AcademiaDoZe.Presentation.AppMaui.Views.LogradouroPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:AcademiaDoZe.Presentation.AppMaui.ViewModels"
    Title="Cadastro de Logradouro">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <!-- CEP -->
            <Label Text="CEP" FontAttributes="Bold" />
            <Entry Text="{Binding Logradouro.Cep}" Placeholder="Digite o CEP" />

            <!-- Nome -->
            <Label Text="Nome" FontAttributes="Bold" />
            <Entry Text="{Binding Logradouro.Nome}" Placeholder="Rua, Avenida, etc" />

            <!-- Bairro -->
            <Label Text="Bairro" FontAttributes="Bold" />
            <Entry Text="{Binding Logradouro.Bairro}" Placeholder="Ex: Centro" />

            <!-- Cidade -->
            <Label Text="Cidade" FontAttributes="Bold" />
            <Entry Text="{Binding Logradouro.Cidade}" Placeholder="Ex: São Paulo" />

            <!-- Estado -->
            <Label Text="Estado" FontAttributes="Bold" />
            <Entry Text="{Binding Logradouro.Estado}" Placeholder="SP" MaxLength="2" />

            <!-- País -->
            <Label Text="País" FontAttributes="Bold" />
            <Entry Text="{Binding Logradouro.Pais}" Placeholder="Brasil" />

            <!-- Botão Salvar -->
            <Button
                Text="Salvar"
                Command="{Binding SaveLogradouroCommand}"
                BackgroundColor="#2196F3"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="45" />

            <!-- Botão Cancelar -->
            <Button
                Text="Cancelar"
                Command="{Binding CancelCommand}"
                BackgroundColor="#E53935"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="45" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
